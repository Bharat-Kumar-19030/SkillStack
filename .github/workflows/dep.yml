on:
  push
jobs:
  build:  
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: cd backend
      - run: npm install
      - run: echo 'npm installed'
      - run: npm build