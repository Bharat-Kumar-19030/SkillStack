pipeline {
    agent any

    stages {

        stage('Build') {
            steps {
                echo 'Build Started'
            }
        }

        stage('Test') {
            steps {
                echo 'Running Tests by bharat'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deployment Complete'
            }
        }

    }
}